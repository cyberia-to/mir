//! Edge rendering: bundled tubes + flow UV animation. Step 9.

/// Per-frame UV offset update for flow-strip texture.
/// v = sign(A^eff_2) * |P_pq| (R-1.0 §8.2).
pub fn update_flow_uvs(_uvs: &mut [f32], _dt: f32) {
    todo!("edge flow UV animation — step 9")
}
